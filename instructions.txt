conda activate teja_streaming_2
pip install mosaicml-streaming
pip install mosaicml
pip install einops
pip install pandas
ppi install accelerate
pip install sentencepiece
pip install 'mosaicml[tensorboard]'

python data_gen/csv_generation.py
accelerate launch stream_flux.py 
composer -n 3 fluxtrain.py 

# to kill zombie process
ps aux | grep fluxtrain.py
pkill -f fluxtrain.py

# since some of the gpus are always busy
export CUDA_VISIBLE_DEVICES=4,5,7
export CUDA_VISIBLE_DEVICES=4

# to monitor the tensorboard
tensorboard --logdir='train/output/tensorboard_logs/flux_training_run'